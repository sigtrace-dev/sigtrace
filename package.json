{
  "name": "sigtrace-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "Universal Reactive Graph & Signal Lifecycle Tracer",
  "workspaces": [
    "packages/*",
    "demo"
  ],
  "scripts": {
    "compile": "tsc -p packages/core && tsc -p packages/vite-plugin && tsc -p packages/extension",
    "clean": "rm -rf packages/core/dist packages/vite-plugin/dist packages/extension/dist"
  },
  "devDependencies": {
    "@angular/core": "^22.0.6",
    "typescript": "^5.0.0"
  }
}
